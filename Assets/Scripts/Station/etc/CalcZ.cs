using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CalcZ : MonoBehaviour
{
/*    float previousY;
    float scaleFactor = 0.1f;
    float threshold = 0.01f;

    // Start is called before the first frame update
    void Start()
    {
        previousY = transform.position.y;
        float newZ = transform.position.y * scaleFactor;
        transform.position = new Vector3(transform.position.x, transform.position.y, newZ);
    }

    void LateUpdate()
    {
        float currentY = transform.position.y;
        if (Mathf.Abs(currentY - previousY) > threshold)
        {
            float newZ = currentY * scaleFactor;
            transform.position = new Vector3(transform.position.x, currentY, newZ);
            previousY = currentY;
        }
    }*/
}
